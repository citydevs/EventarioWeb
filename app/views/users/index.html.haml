%h1 Listing users

%table
  %thead
    %tr
      %th Email
      %th Admin
      %th Ver
      %th
      %th

  - @users.each do |user|
    %tr
      %td= user.email
      %td= user.admin?
      %td= link_to 'Ver', user
      %td= link_to 'Editar', edit_user_path(user)
      %td= link_to 'Eliminar', user, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

//= link_to 'New User', new_user_path
